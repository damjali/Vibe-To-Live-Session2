import asyncio
import uuid
import json

async def test():
    # Write a python websocket client without using websockets library.
    # Actually, uvicorn is running, I'll just use curl to test if port 8001 is responding to HTTP.
    pass
